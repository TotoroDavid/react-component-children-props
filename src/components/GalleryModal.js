import React from 'react'
import Modal from './Modal'

const GalleryModal = (props) => {
    return (
        <Modal
            {...props}
            title='new gallery'
        />
    )
}

export default GalleryModal
